version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    post_install:
      - pip install --group docs .

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - method: pip
      path: .
